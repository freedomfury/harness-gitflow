trigger:
  name: ops push
  identifier: ops_build_push
  enabled: true
  orgIdentifier: ${org_id}
  projectIdentifier: ${project_id}
  pipelineIdentifier: ops_build
  source:
    type: Webhook
    spec:
      type: Harness
      spec:
        type: Push
        spec:
          connectorRef: ""
          autoAbortPreviousExecutions: true
          payloadConditions:
            - key: targetBranch
              operator: StartsWith
              value: "ops/"
          headerConditions: []
          repoName: image-build
          actions: []
  inputYaml: |
    pipeline:
      identifier: ops_build
      properties:
        ci:
          codebase:
            build:
              type: branch
              spec:
                branch: <+trigger.branch>
