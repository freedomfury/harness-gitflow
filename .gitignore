# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfstate.backup
terraform.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Auto-generated SDK clients (regenerated via openapi-python-client; see docs/sdk-generation.md).
# The CLI itself (harness_cli/) is hand-written and builds commands at runtime.
tool/harness-code-api-client/
tool/harness-pipeline-api-client/
tool/harness-platform-api-client/
# Vendored SDK copies for the standalone harness-cli package — generated from
# the specs (examples/) via tool/harness-cli/scripts/vendor.sh. Gitignored AND
# excluded from the tarball; the recipient regenerates. Same policy as above.
tool/harness-cli/vendor/
tool/wheels/

# Credentials
exports
.zai.env

# Local tool state (Claude Code, editor state)
.claude/
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db
